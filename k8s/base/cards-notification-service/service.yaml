apiVersion: v1
kind: Service
metadata:
  name: cards-notification-service
  labels:
    app: cards-notification-service
spec:
  type: ClusterIP
  selector:
    app: cards-notification-service
  ports:
    - name: http
      port: 8084
      targetPort: http
