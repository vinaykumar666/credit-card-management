apiVersion: apps/v1
kind: Deployment
metadata:
  name: cards-notification-service
  labels:
    app: cards-notification-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cards-notification-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: cards-notification-service
    spec:
      terminationGracePeriodSeconds: 40
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app: cards-notification-service
                topologyKey: kubernetes.io/hostname
      containers:
        - name: cards-notification-service
          image: cards-notification-service:1.0.0
          ports:
            - name: http
              containerPort: 8084
          envFrom:
            - configMapRef:
                name: cards-notification-service
            - secretRef:
                name: cards-notification-service
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: http
            initialDelaySeconds: 40
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: http
            initialDelaySeconds: 20
            periodSeconds: 5
