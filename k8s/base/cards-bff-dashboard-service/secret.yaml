apiVersion: v1
kind: Secret
metadata:
  name: cards-bff-dashboard-service
  labels:
    app: cards-bff-dashboard-service
type: Opaque
stringData:
  # Placeholder for future BFF-specific secrets (e.g. client credentials).
  PLACEHOLDER: CHANGE_ME
