apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: cards-payment-service
  labels:
    app: cards-payment-service
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app: cards-payment-service
