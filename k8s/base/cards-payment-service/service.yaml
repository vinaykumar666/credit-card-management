apiVersion: v1
kind: Service
metadata:
  name: cards-payment-service
  labels:
    app: cards-payment-service
spec:
  type: ClusterIP
  selector:
    app: cards-payment-service
  ports:
    - name: http
      port: 8083
      targetPort: http
